[package]
name = "gwz-transport"
version = "0.0.0-bootstrap.1"
edition = "2024"
license = "GPL-2.0-only"
description = "Bootstrap prerelease for the actively developed gwz-transport project; no implementation included."
repository = "https://github.com/owebeeone/gwz-transport"
readme = "README.md"
publish = ["crates-io"]
include = ["Cargo.toml", "Cargo.lock", "README.md", "src/lib.rs"]

# Keep this dependency-free bootstrap separate from the production workspace.
[workspace]
